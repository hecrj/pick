[package]
name = "pick-core"
version = "0.1.0-dev"
edition = "2024"

[dependencies]
decoder = "0.0.3"
reason = "0.2.0-dev"
serde_json = "1"

tokio.version = "1"
tokio.features = ["fs", "rt", "sync"]

[dev-dependencies]
tokio.version = "1"
tokio.features = ["macros", "test-util"]
